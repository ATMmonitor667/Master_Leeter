MASTER LEETER — PRODUCTION IMPLEMENTATION TODO
Prepared: 2026-09-11
Branch: production
Audited commit: b4c25c4
Status: ITERATION 1 implemented locally; external acceptance remains open.

CURRENT EXECUTION ORDER (supersedes older product/provider proposals below)
[x] I01 Private Supabase bank migration, adapter, importer and session wiring.
[ ] I01-CLOUD Owner applies development migration/import and verifies permissions.
[~] I02 Authentication, ownership, durable sessions/events and private-data lifecycle
    are implemented; live Supabase and multi-instance acceptance remain.
    I02a auth committed (dc5a1a7); configuration/reconnect hardening follows.
    I02b durable storage/recovery remains open; single API process only for now.
    I02b.1 repository foundation implemented; real PostgreSQL tests and API wiring
    remain open. Resume instructions: NEXT_IMPLEMENTATION_PLAN.txt.
    I02b.2 pg driver/integration harness/CI job implemented; six live DB tests
    await PostgreSQL or CI execution. See docs/DATABASE_TESTING.md.
[x] I02a Supabase email-code sign-in, verified ownership and session-bound sockets.
[ ] I02a-CLOUD Real sign-in/email/token-expiry acceptance with development project.
[~] I02b Durable storage, atomic lifecycle, replay obligations, fenced runtime
    ownership and report/privacy recovery are implemented. Live database and
    cross-process acceptance remain.
[~] I03 production code implemented on codex-production-03-preparation; automated,
    live Supabase/Gemini and human conversational acceptance remain open.
[~] I04 core code implemented on codex-production-04-live-experience: minimal
    codepad/notes UI, provider input transcription, voice action/reconnect path,
    acknowledged saves, atomic final cursor seal, late-write rejection and server
    deadline/abandonment completion, GoAway rotation, provider resumption/context
    compression, audio preflight/device recovery and final spoken-turn drain.
    Real 45-minute browser/device/cloud acceptance remains.
[~] I05 Independent 0-100 solution/transcript Gemini graders, fenced partial
    checkpoints/retries and separate evidence-backed report cards implemented.
    Migration, calibration fixtures and live/human grading acceptance remain.
[~] I06 release boundary plus durable global/account admission, kill switch,
    provider-route rates and voice credential caps implemented on
    codex-production-06-launch-hardening. Safe capability status, provider
    breakers, alertable redacted signals, exact-image CI and a staging blueprint
    are implemented. Redacted critical webhook delivery is implemented;
    configured paging drills, backup/restore, hosted checks and the owner-assisted
    pilot remain.
[~] I07 product completion on codex-production-07-product-completion: durable,
    owner-scoped interview history plus resume/report navigation implemented;
    settings and export are implemented. Verified application-schema backup and
    isolated restore tooling is implemented; accessibility review, runner,
    live erasure-ledger replay and recovery drills remain. The release scope is
    a free capped beta with model-estimated grades; sandbox execution is deferred.
Full per-iteration tasks, acceptance and owner dependencies:
  docs/PRODUCTION_ITERATIONS.md
Question bank setup (no cloud data has been written):
  docs/SUPABASE_QUESTION_BANK.md
Each iteration gets a tested commit, then owner review/merge to production.
The detailed P-tasks below remain open unless explicitly closed with evidence.

Detailed scope, dependencies, owners and acceptance:
  docs/PRODUCTION_PLAN.md
Vercel settings, backend hosting, environments, rollout and rollback:
  docs/VERCEL_DEPLOYMENT.md

Historical TODO.txt describes the completed personal-MVP pass. This is the new
production backlog. Close a task only with a linked commit/test/deployment or
human acceptance record. A green frontend build alone does not close launch.

RECOMMENDED HOSTING
  Vercel: Next.js web app.
  Render paid service: Fastify HTTP + application WebSocket API.
  Render worker: durable execution dispatch and report processing.
  Managed PostgreSQL + Redis-compatible coordination; Clerk proposed for auth.
  External sandbox for actual Python execution; choose after isolation tests.
  Gemini: account-verified speech/classifier configuration with full recovery.
  Alternative API-on-Vercel spike is documented; WebSockets now exist in beta
  but function duration and instance turnover require proven recovery.

FOUNDATION / ACCESS
[ ] P01 Production specification, provider/budget decisions, documentation audit.
[~] P02 Reproducible Linux API artifact, contracts, assets and start script are
    implemented; worker artifact/start path and a successful CI image run remain.
[~] P03 Typed config, production validation, readiness and browser endpoints are
    implemented; hosted configuration and probe evidence remain.
[~] P04 Supabase authentication and browser session refresh are implemented;
    server-side Auth identity deletion is implemented after practice-data erasure;
    live expiry/revocation/deletion acceptance remains.
[~] P05 HTTP/WS ownership and durable single-use socket tickets are implemented;
    two-account hosted acceptance remains.
[~] P06 Exact origins, bounded inputs, shared route rates and browser headers are
    implemented; hosted abuse/browser acceptance remains.

DATA / RECOVERY
[~] P07 PostgreSQL repositories, numbered migration runner, preparations and
    history are implemented; managed migration execution remains.
[~] P08 Transactional event sequencing, lifecycle, idempotency and durable input
    obligations are implemented; live concurrency acceptance remains.
[~] P09 Runtime reconstruction, checkpoints and pending-input discovery are
    implemented; forced replacement acceptance remains.
[~] P10 Fenced runtime ownership and durable report/privacy jobs are implemented;
    cross-instance command and replacement acceptance remain.

INTERVIEW / EXECUTION / REPORTS
[~] P11 Provider transcription is independent of captions; completion waits for
    playback drain and a bounded final candidate transcript drain. Browser audio
    preflight plus microphone/speaker switching and recovery are implemented;
    human hardware acceptance remains.
[~] P12 Fresh-token reconnect, provider GoAway rotation, server-held resumption
    handles and context compression are implemented. A real 45-minute provider
    verification remains.
[~] P13 Durable server deadline discovery, atomic finalization, late-write rejection
    and abandoned-tab completion are implemented; cloud race drills remain.
[>] P14–P16 External sandbox execution deferred from the selected free capped beta;
    model-estimated feedback remains explicitly labelled and Run is not required.
[~] P17 Durable report jobs, fenced recovery, persisted partial results/retries and
    deletion are implemented; live failure/deletion-race acceptance remains.
[~] P18 Independent grounded graders and observer wiring are implemented; live
    calibration and human review remain.

PRODUCT / PRIVACY / OPERATIONS
[~] P19 Owner-scoped session export/deletion and account practice-data deletion
    are implemented. Durable deletion recovery, 365-day automatic expiry and
    external restore-replay artifacts and Auth identity deletion are implemented;
    reviewed policy copy and live replay evidence remain.
[~] P20 Owner-scoped history, account/public surfaces, optional-resume launch,
    skip navigation, keyboard radio groups, trapped/Escape dialogs, status/error
    announcements and reduced motion are implemented; real screen-reader,
    contrast and supported-device acceptance remain.
[~] P21 Atomic monthly/account/global admission, shared rate limits, provider
    breakers, voice mint caps and kill switch are implemented; hard provider
    voice-lifetime enforcement and measured cost acceptance remain.
[~] P22 Redacted service status, bounded critical webhook alerts and durable,
    rate-limited in-session diagnostic reports with 30-day expiry are implemented;
    configure escalation/support ownership and prove delivery on staging.
[ ] P23 Real DB/queue, browser, two-instance, load and human voice acceptance.
[~] P24 Production CI builds/smokes the API image; migration execution,
    compatibility and hosted release evidence remain.
[~] P25 Render staging blueprint and Vercel configuration are documented;
    provisioning, protection, worker/domain wiring and hosted evidence remain.
[~] P26 Verified public-schema archive and guarded isolated restore commands are
    implemented with migration controls, erasure-ledger export/replay and a
    recovery/forward-rollback runbook. Supabase managed backup/PITR, live restore,
    deployment rollback and incident rehearsal remain.

LAUNCH / GROWTH
[ ] P27 Invite 5–10 users; observe ten full interviews and fix activation failures.
[~] P28 Account-aware repetition avoidance is implemented; expand to 12–20
    reviewed original/licensed scenarios for repeat use.
[ ] P29 Meet public-beta reliability, capacity, quality and cost thresholds.
[~] P30 Truthful public/discovery surfaces and a read-only aggregate activation,
    completion, recovery, support and report-latency command are implemented;
    host/Supabase visit/signup metrics, demo and two cohorts remain.
[ ] P31 Gradual public signup, first-48-hour monitoring and operational ownership.
[>] P32 Billing is deferred; the selected release is a free capped beta.

ORDER NOTES
  Begin P01–P03, then P07/P08 and P04–P06, then P09/P10.
  P14 sandbox spike can run alongside storage work; P11/P12 is a critical risk.
  Start release plumbing from P24/P25 early to give P23 a protected test host.
  Finish P24 after P23 passes; staging exists before release qualification.
  Detailed task dependencies in the plan take precedence over numeric order.

RELEASE GATES
[ ] G0 Protected staging: runnable, authenticated, persistent hosted vertical slice.
[ ] G1 Invite beta: P01–P27 and ten real full interviews, including 45 minutes.
[ ] G2 Public beta: P28–P31 and 20 full sessions across at least ten people.
[ ] G3 Paid launch, if applicable: P32 plus verified cost/support controls.

OWNER DECISIONS TO RECORD DURING P01
  Domain; launch region; monthly spend ceiling; maximum concurrent interviews;
  free-credit offer; supported devices; retention; identity and sandbox choices.
  No accounts, payments, deployment or public promotion were performed by this plan.
